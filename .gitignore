# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Git worktrees (isolated workspaces for feature work)
.worktrees/

# Impeccable design skill — local/ephemeral only; design.json and
# live/config.json are real project config and stay tracked.
.impeccable/config.local.json
.impeccable/hook.cache.json
.impeccable/live/roots.json
.impeccable/live/sessions/
.impeccable/live/accept-receipts/

# Environment variables
.env
.env.local
.env.production

# Supabase
.supabase
supabase/.temp/

# WhatsApp bot (local dev runs only — production secrets live in Fly.io)
whatsapp-bot/node_modules
whatsapp-bot/baileys-auth
whatsapp-bot/.env


